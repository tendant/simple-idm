# Simple IDM Environment Configuration (for Makefile)
# Copy this file to .env and update the values for your environment
#
# This file is primarily used by the Makefile for database operations
# (migrations, dumps). For full application configuration, see cmd/loginv2/.env.example

# =============================================================================
# DATABASE CONFIGURATION (for Makefile operations)
# =============================================================================

# PostgreSQL Database Connection
IDM_PG_HOST=localhost
IDM_PG_PORT=5432
IDM_PG_DATABASE=idm_db
IDM_PG_USER=idm
IDM_PG_PASSWORD=pwd
IDM_PG_SCHEMA=public

# =============================================================================
# NOTES
# =============================================================================

# These variables are used by Makefile targets:
# - migration-create, migration-up, migration-down (goose migrations)
# - dump-idm, dump-db (pg_dump operations)
#
# If this .env file doesn't exist, the Makefile will use the default values
# shown above.
#
# For full application configuration, see cmd/loginv2/.env.example
