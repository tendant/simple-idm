# Simple IDM Deployment Configuration

# Domain
DOMAIN=idm.example.com

# Backend port
BACKEND_PORT=4000

# Web root directory
WEB_ROOT=/var/www/idm.example.com/html

# Log file prefix (used for /var/log/nginx/${LOG_PREFIX}_access.log)
LOG_PREFIX=idm

# SSL certificate paths (managed by certbot)
SSL_CERT=/etc/letsencrypt/live/idm.example.com/fullchain.pem
SSL_KEY=/etc/letsencrypt/live/idm.example.com/privkey.pem
