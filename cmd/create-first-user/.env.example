# Simple IDM Create First User Configuration
# Copy this file to .env and update the values for your environment

# Database Configuration (Option 1: Use DATABASE_URL)
# DATABASE_URL=postgresql://username:password@localhost:5432/simple_idm

# Database Configuration (Option 2: Individual variables)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=simple_idm
DB_USER=postgres
DB_PASSWORD=your_password_here

# Script Configuration (Optional)
# DEFAULT_USERNAME=super
# DEFAULT_EMAIL_DOMAIN=example.com

# Notes:
# - If DATABASE_URL is set, individual DB_* variables will be ignored
# - DEFAULT_USERNAME: Default username when none is provided as argument
# - DEFAULT_EMAIL_DOMAIN: Default domain for email when only username is provided
# - Environment variables can still override .env file values
# - This file should be added to .gitignore for security
